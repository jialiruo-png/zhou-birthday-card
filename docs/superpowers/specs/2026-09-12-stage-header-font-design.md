# Stage 顶部章节标题字体调整设计

## 目标

仅将 Stage 顶部章节名称（如「先看见周老师」「人物序章」「文字心意」「师生故事」）的字体改为「周洁红教授 · 治学长卷」标题使用的宋体字体栈。

## 范围

- 仅修改 `.stage-header h2` 的 `font-family`。
- 保持原有字号、字重、字距、间距、颜色和位置不变。
- 不修改场景内主标题、开场标题、照片标题、结束页或手机端。

## 验收

- Stage 顶部全部章节名称使用宋体字体栈。
- 顶部章节名称的尺寸和布局与修改前一致。
